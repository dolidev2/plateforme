{% extends 'base.html.twig' %}

{% block title %} {{ service.nomService }} | {{ dossiers.nomDossier }} | Ajouter devis{% endblock %}

{% block content %}
    <div style="margin-left: 40%;">
        <button type="button" class="btn btn-primary btn-rounded btn-fw">{{ service.nomService }}</button>
    </div>
    <div class="row"  style="margin-left: 30%; margin-top: 10px; margin-bottom: 10px;">

        <div class="col-md-3 col-lg-4 col-sm-4">
            <a href="{{ path('app_service',{id:service.id }) }}"><button type="button" class="btn btn-success btn-rounded btn-fw">Dossier</button></a>
        </div>

        <div class="col-md-3 col-lg-4 col-sm-4">
            <a href="{{ path('app_service_cloturer_dossier',{service:service.id}) }}"><button type="button" class="btn btn-warning btn-rounded btn-fw">Clôturer</button></a>
        </div>

        <div class="col-md-3 col-lg-4 col-sm-4">
            <a href="{{ path('app_service_historique_dossier',{service:service.id}) }}"><button type="button" class="btn btn-info btn-rounded btn-fw">Historique</button></a>
        </div>

    </div>
    <br>
    <br>
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Ajouter Un  Devis</h4>
                {{ form_start(formDev) }}
                {{ form_widget(form) }}
                {{ form_widget(formFour) }}
                {{ form_widget(formDev) }}
                <button type="submit" class="btn btn-primary mr-2">Ajouter</button>
                <button class="btn btn-light">Annuler</button>

                {{ form_end(formDev) }}

            </div>
        </div>
    </div>
{% endblock %}